# Implementation decisions and rework costs

Date: 2026-08-31. These are the implementation ledger's rulings, retained in their original order so the decisions survive the temporary task workspace. They do not expand permission to merge, push, deploy, submit forms or bypass browser restrictions.

1. Ruling: Work in the existing dedicated feature checkout while the optional worktree question is unanswered — the user authorized implementation, but not a new checkout; original export stays unchanged — if wrong, generated source changes must be moved into a later worktree.

2. Ruling: Preserve the approved red sedan hero rather than the plan's graphite coupe prompt — the selected visual is authoritative — if wrong, only hero image regeneration is needed.

3. Ruling: Keep the existing-project static architecture rather than bootstrap a new Product Design Vite app — the approved spec explicitly rejects framework migration — if wrong, build plumbing would require rework.

4. Ruling: Generate references while independent domain/build-input tasks run, with scoped file ownership and serialized commits — higher-level collaboration guidance encourages useful parallel work — if wrong, integration corrections are local and reviewable.

5. Ruling: Assert emitted HTML/HEAD Content-Length at the native response and CSS Content-Length over HTTP — a minimal native server proves this environment rewrites only HTML responses to chunked transfer and injects Permissions-Policy — if wrong, the HTML transport assertion must be restored after isolating the actual transport defect. Do not claim end-to-end HTML Content-Length preservation.

6. Ruling: Reuse an available completed worker for the bounded checker recovery when both original-worker resumption and fresh-agent dispatch fail with a thread-limit error — local implementation can continue with exact ownership and independent subsequent review — if wrong, repeat the checker review in a fresh context once capacity is available.

The checker recovery subsequently received fresh independent scoped approval. The existing feature branch and temporary task ledger are retained while browser acceptance remains incomplete.
